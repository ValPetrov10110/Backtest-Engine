# Backtest-Engine

### Known Improvements
- Don't keep the FetchContent_Declare() to have GIT_TAG master long-term
- Maybe include .setTicker inside of a run backtest or something? Will work on this, just keep it in mind
